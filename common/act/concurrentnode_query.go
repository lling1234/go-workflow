// Code generated by ent, DO NOT EDIT.

package act

import (
	"act/common/act/concurrentnode"
	"act/common/act/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ConcurrentNodeQuery is the builder for querying ConcurrentNode entities.
type ConcurrentNodeQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ConcurrentNode
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ConcurrentNodeQuery builder.
func (cnq *ConcurrentNodeQuery) Where(ps ...predicate.ConcurrentNode) *ConcurrentNodeQuery {
	cnq.predicates = append(cnq.predicates, ps...)
	return cnq
}

// Limit adds a limit step to the query.
func (cnq *ConcurrentNodeQuery) Limit(limit int) *ConcurrentNodeQuery {
	cnq.limit = &limit
	return cnq
}

// Offset adds an offset step to the query.
func (cnq *ConcurrentNodeQuery) Offset(offset int) *ConcurrentNodeQuery {
	cnq.offset = &offset
	return cnq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cnq *ConcurrentNodeQuery) Unique(unique bool) *ConcurrentNodeQuery {
	cnq.unique = &unique
	return cnq
}

// Order adds an order step to the query.
func (cnq *ConcurrentNodeQuery) Order(o ...OrderFunc) *ConcurrentNodeQuery {
	cnq.order = append(cnq.order, o...)
	return cnq
}

// First returns the first ConcurrentNode entity from the query.
// Returns a *NotFoundError when no ConcurrentNode was found.
func (cnq *ConcurrentNodeQuery) First(ctx context.Context) (*ConcurrentNode, error) {
	nodes, err := cnq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{concurrentnode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cnq *ConcurrentNodeQuery) FirstX(ctx context.Context) *ConcurrentNode {
	node, err := cnq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ConcurrentNode ID from the query.
// Returns a *NotFoundError when no ConcurrentNode ID was found.
func (cnq *ConcurrentNodeQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = cnq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{concurrentnode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cnq *ConcurrentNodeQuery) FirstIDX(ctx context.Context) int64 {
	id, err := cnq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ConcurrentNode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ConcurrentNode entity is found.
// Returns a *NotFoundError when no ConcurrentNode entities are found.
func (cnq *ConcurrentNodeQuery) Only(ctx context.Context) (*ConcurrentNode, error) {
	nodes, err := cnq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{concurrentnode.Label}
	default:
		return nil, &NotSingularError{concurrentnode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cnq *ConcurrentNodeQuery) OnlyX(ctx context.Context) *ConcurrentNode {
	node, err := cnq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ConcurrentNode ID in the query.
// Returns a *NotSingularError when more than one ConcurrentNode ID is found.
// Returns a *NotFoundError when no entities are found.
func (cnq *ConcurrentNodeQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = cnq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{concurrentnode.Label}
	default:
		err = &NotSingularError{concurrentnode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cnq *ConcurrentNodeQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := cnq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ConcurrentNodes.
func (cnq *ConcurrentNodeQuery) All(ctx context.Context) ([]*ConcurrentNode, error) {
	if err := cnq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return cnq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (cnq *ConcurrentNodeQuery) AllX(ctx context.Context) []*ConcurrentNode {
	nodes, err := cnq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ConcurrentNode IDs.
func (cnq *ConcurrentNodeQuery) IDs(ctx context.Context) ([]int64, error) {
	var ids []int64
	if err := cnq.Select(concurrentnode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cnq *ConcurrentNodeQuery) IDsX(ctx context.Context) []int64 {
	ids, err := cnq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cnq *ConcurrentNodeQuery) Count(ctx context.Context) (int, error) {
	if err := cnq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return cnq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (cnq *ConcurrentNodeQuery) CountX(ctx context.Context) int {
	count, err := cnq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cnq *ConcurrentNodeQuery) Exist(ctx context.Context) (bool, error) {
	if err := cnq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return cnq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (cnq *ConcurrentNodeQuery) ExistX(ctx context.Context) bool {
	exist, err := cnq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ConcurrentNodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cnq *ConcurrentNodeQuery) Clone() *ConcurrentNodeQuery {
	if cnq == nil {
		return nil
	}
	return &ConcurrentNodeQuery{
		config:     cnq.config,
		limit:      cnq.limit,
		offset:     cnq.offset,
		order:      append([]OrderFunc{}, cnq.order...),
		predicates: append([]predicate.ConcurrentNode{}, cnq.predicates...),
		// clone intermediate query.
		sql:    cnq.sql.Clone(),
		path:   cnq.path,
		unique: cnq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ProcInstID int64 `json:"proc_inst_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ConcurrentNode.Query().
//		GroupBy(concurrentnode.FieldProcInstID).
//		Aggregate(act.Count()).
//		Scan(ctx, &v)
//
func (cnq *ConcurrentNodeQuery) GroupBy(field string, fields ...string) *ConcurrentNodeGroupBy {
	grbuild := &ConcurrentNodeGroupBy{config: cnq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := cnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return cnq.sqlQuery(ctx), nil
	}
	grbuild.label = concurrentnode.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ProcInstID int64 `json:"proc_inst_id,omitempty"`
//	}
//
//	client.ConcurrentNode.Query().
//		Select(concurrentnode.FieldProcInstID).
//		Scan(ctx, &v)
//
func (cnq *ConcurrentNodeQuery) Select(fields ...string) *ConcurrentNodeSelect {
	cnq.fields = append(cnq.fields, fields...)
	selbuild := &ConcurrentNodeSelect{ConcurrentNodeQuery: cnq}
	selbuild.label = concurrentnode.Label
	selbuild.flds, selbuild.scan = &cnq.fields, selbuild.Scan
	return selbuild
}

func (cnq *ConcurrentNodeQuery) prepareQuery(ctx context.Context) error {
	for _, f := range cnq.fields {
		if !concurrentnode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("act: invalid field %q for query", f)}
		}
	}
	if cnq.path != nil {
		prev, err := cnq.path(ctx)
		if err != nil {
			return err
		}
		cnq.sql = prev
	}
	return nil
}

func (cnq *ConcurrentNodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ConcurrentNode, error) {
	var (
		nodes = []*ConcurrentNode{}
		_spec = cnq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*ConcurrentNode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &ConcurrentNode{config: cnq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cnq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cnq *ConcurrentNodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cnq.querySpec()
	_spec.Node.Columns = cnq.fields
	if len(cnq.fields) > 0 {
		_spec.Unique = cnq.unique != nil && *cnq.unique
	}
	return sqlgraph.CountNodes(ctx, cnq.driver, _spec)
}

func (cnq *ConcurrentNodeQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := cnq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("act: check existence: %w", err)
	}
	return n > 0, nil
}

func (cnq *ConcurrentNodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   concurrentnode.Table,
			Columns: concurrentnode.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: concurrentnode.FieldID,
			},
		},
		From:   cnq.sql,
		Unique: true,
	}
	if unique := cnq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := cnq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, concurrentnode.FieldID)
		for i := range fields {
			if fields[i] != concurrentnode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cnq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cnq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cnq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cnq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cnq *ConcurrentNodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cnq.driver.Dialect())
	t1 := builder.Table(concurrentnode.Table)
	columns := cnq.fields
	if len(columns) == 0 {
		columns = concurrentnode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cnq.sql != nil {
		selector = cnq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cnq.unique != nil && *cnq.unique {
		selector.Distinct()
	}
	for _, p := range cnq.predicates {
		p(selector)
	}
	for _, p := range cnq.order {
		p(selector)
	}
	if offset := cnq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cnq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ConcurrentNodeGroupBy is the group-by builder for ConcurrentNode entities.
type ConcurrentNodeGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cngb *ConcurrentNodeGroupBy) Aggregate(fns ...AggregateFunc) *ConcurrentNodeGroupBy {
	cngb.fns = append(cngb.fns, fns...)
	return cngb
}

// Scan applies the group-by query and scans the result into the given value.
func (cngb *ConcurrentNodeGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := cngb.path(ctx)
	if err != nil {
		return err
	}
	cngb.sql = query
	return cngb.sqlScan(ctx, v)
}

func (cngb *ConcurrentNodeGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range cngb.fields {
		if !concurrentnode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cngb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cngb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cngb *ConcurrentNodeGroupBy) sqlQuery() *sql.Selector {
	selector := cngb.sql.Select()
	aggregation := make([]string, 0, len(cngb.fns))
	for _, fn := range cngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(cngb.fields)+len(cngb.fns))
		for _, f := range cngb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(cngb.fields...)...)
}

// ConcurrentNodeSelect is the builder for selecting fields of ConcurrentNode entities.
type ConcurrentNodeSelect struct {
	*ConcurrentNodeQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (cns *ConcurrentNodeSelect) Scan(ctx context.Context, v interface{}) error {
	if err := cns.prepareQuery(ctx); err != nil {
		return err
	}
	cns.sql = cns.ConcurrentNodeQuery.sqlQuery(ctx)
	return cns.sqlScan(ctx, v)
}

func (cns *ConcurrentNodeSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := cns.sql.Query()
	if err := cns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
